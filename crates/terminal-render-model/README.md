# terminal-render-model

Renderer-neutral viewport and cell model for Hera.

This crate contains the public rendering data structures consumed by tests,
debug tools and future host adapters. It stays independent from GPU, windowing
and terminal runtime code.
