# terminal-cli

Debug, replay and evidence command-line tooling for Hera.

This crate provides local commands for byte injection, PTY runs, fixture replay,
milestone evidence generation and validation. It is a readiness tool, not a
publishing automation surface.
